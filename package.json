{
  "private": true,
  "name": "@constexprcss/core",
  "dependencies": {
    "@changesets/changelog-github": "^0.4.2",
    "eslint-scope": "^5.1.1",
    "mime-types": "^2.1.32",
    "typescript": "^4.4.2"
  },
  "devDependencies": {
    "@changesets/cli": "^2.19.0"
  },
  "scripts": {
    "build": "pnpm build --parallel",
    "build:babel": "pnpm build --filter @constexprcss/babel-plugin --parallel",
    "build:vite": "pnpm build --filter @constexprcss/vite --parallel",
    "changeset": "changeset",
    "release": "pnpm publish --no-git-checks --filter=!@constexprcss/core"
  }
}
